var myAge; // Declare variable
var yearsToAdd = 49; // Initialize variable
myAge=32; // Assigning a value to a variable

// Create a function that adds years to myAge
function addYears () {
console.log("my Age is: "+ myAge);
console.log("In" + yearsToAdd + " Years I will be:");
console.log(myAge + yearsToAdd);
}
 
addYears();
